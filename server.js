const express = require('express'); const mongoose = require('mongoose'); const jwt = require('jsonwebtoken'); const nodemailer = require('nodemailer'); const app = express(); app.use(express.json()); mongoose.connect('mongodb://localhost:27017/taskmanager', { useNewUrlParser: true, useUnifiedTopology: true }); const UserSchema = new mongoose.Schema({ email: String, password: String }); const TaskSchema = new mongoose.Schema({ title: String, description: String, due_date: Date, completed: { type: Boolean, default: false } }); const User = mongoose.model('User', UserSchema); const Task = mongoose.model('Task', TaskSchema); app.post('/api/auth/register', async (req, res) => { const user = new User(req.body); await user.save(); res.send({ status: 'success', message: 'User registered, confirmation email sent.' }); }); app.post('/api/auth/login', async (req, res) => { const user = await User.findOne(req.body); if (user) { const token = jwt.sign({ id: user._id }, 'secret'); res.send({ status: 'success', token }); } else { res.status(401).send({ status: 'error', message: 'Invalid credentials' }); } }); app.post('/api/tasks', async (req, res) => { const task = new Task(req.body); await task.save(); res.send({ status: 'success', task_id: task._id }); }); app.get('/api/tasks', async (req, res) => { const tasks = await Task.find(); res.send({ status: 'success', tasks }); }); app.put('/api/tasks/:id', async (req, res) => { await Task.findByIdAndUpdate(req.params.id, req.body); res.send({ status: 'success', message: 'Task updated.' }); }); app.delete('/api/tasks/:id', async (req, res) => { await Task.findByIdAndDelete(req.params.id); res.send({ status: 'success', message: 'Task deleted.' }); }); app.patch('/api/tasks/:id/complete', async (req, res) => { await Task.findByIdAndUpdate(req.params.id, { completed: true }); res.send({ status: 'success', message: 'Task marked as complete.' }); }); app.get('/api/tasks/filter', async (req, res) => { const tasks = await Task.find({ completed: req.body.status === 'completed' }); res.send({ status: 'success', tasks }); }); app.post('/api/notifications/settings', (req, res) => { res.send({ status: 'success', message: 'Notification settings updated.' }); }); app.listen(3000, () => { console.log('Server running on port 3000'); });