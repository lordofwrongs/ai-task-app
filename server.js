const express = require('express'); const mongoose = require('mongoose'); const jwt = require('jsonwebtoken'); const nodemailer = require('nodemailer'); const app = express(); app.use(express.json()); mongoose.connect('mongodb://localhost:27017/myapp', { useNewUrlParser: true, useUnifiedTopology: true }); const UserSchema = new mongoose.Schema({ email: String, password: String }); const User = mongoose.model('User', UserSchema); const TaskSchema = new mongoose.Schema({ title: String, description: String, category: String, due_date: Date }); const Task = mongoose.model('Task', TaskSchema); app.post('/api/register', async (req, res) => { const user = new User(req.body); await user.save(); res.json({ status: 'success', message: 'User registered' }); }); app.post('/api/login', async (req, res) => { const user = await User.findOne(req.body); if (user) { const token = jwt.sign({ id: user._id }, 'secret'); res.json({ status: 'success', token, message: 'Login successful' }); } else { res.json({ status: 'error', message: 'Invalid credentials' }); } }); app.post('/api/tasks', async (req, res) => { const task = new Task(req.body); await task.save(); res.json({ status: 'success', task_id: task._id, message: 'Task created' }); }); app.put('/api/tasks/:id', async (req, res) => { await Task.findByIdAndUpdate(req.params.id, req.body); res.json({ status: 'success', message: 'Task updated' }); }); app.delete('/api/tasks/:id', async (req, res) => { await Task.findByIdAndDelete(req.params.id); res.json({ status: 'success', message: 'Task deleted' }); }); app.get('/api/tasks', async (req, res) => { const tasks = await Task.find(); res.json({ status: 'success', tasks }); }); app.post('/api/tasks/export', async (req, res) => { // Export logic here res.json({ status: 'success', file_url: 'url_to_file', message: 'Tasks exported' }); }); app.post('/api/tasks/import', async (req, res) => { // Import logic here res.json({ status: 'success', message: 'Tasks imported' }); }); app.listen(3000, () => { console.log('Server running on port 3000'); });