const express = require('express'); const mongoose = require('mongoose'); const nodemailer = require('nodemailer'); const app = express(); app.use(express.json()); mongoose.connect('mongodb://localhost:27017/taskmanager', { useNewUrlParser: true, useUnifiedTopology: true }); const UserSchema = new mongoose.Schema({ email: String, password: String }); const TaskSchema = new mongoose.Schema({ title: String, description: String, due_date: Date }); const User = mongoose.model('User', UserSchema); const Task = mongoose.model('Task', TaskSchema); app.post('/api/auth/register', async (req, res) => { const { email, password } = req.body; const user = new User({ email, password }); await user.save(); res.json({ status: 'success', message: 'User registered' }); }); app.post('/api/auth/login', async (req, res) => { const { email, password } = req.body; const user = await User.findOne({ email, password }); if (user) { res.json({ status: 'success', token: 'dummy_token', message: 'Login successful' }); } else { res.json({ status: 'error', message: 'Invalid credentials' }); }}); app.post('/api/tasks', async (req, res) => { const { title, description, due_date } = req.body; const task = new Task({ title, description, due_date }); await task.save(); res.json({ status: 'success', task_id: task._id, message: 'Task created' }); }); app.put('/api/tasks/:id', async (req, res) => { const { title, description, due_date } = req.body; await Task.findByIdAndUpdate(req.params.id, { title, description, due_date }); res.json({ status: 'success', message: 'Task updated' }); }); app.delete('/api/tasks/:id', async (req, res) => { await Task.findByIdAndDelete(req.params.id); res.json({ status: 'success', message: 'Task deleted' }); }); app.patch('/api/tasks/:id/complete', async (req, res) => { res.json({ status: 'success', message: 'Task marked as complete' }); }); app.get('/api/tasks', async (req, res) => { const tasks = await Task.find(); res.json({ status: 'success', tasks }); }); app.listen(3000, () => { console.log('Server running on port 3000'); });